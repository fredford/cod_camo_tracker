import React from "react";

export default function WeaponHeader({ header }) {
  return (
    <div className="weapon-header-container">
      <h2 className="weapon-header">{header}</h2>
    </div>
  );
}
